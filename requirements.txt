# This requirements file lists all third-party dependencies for this project.
#
# Run 'pip install -r requirements.txt -t lib/' to install these dependencies
# in `lib/` subdirectory.
#
# Note: The `lib` directory is added to `sys.path` by `appengine_config.py`.
Flask==0.10
MySQL-python
python-dateutil

# potential fix: yaml not installed, somehow here locally and not in circleci
# potential issues: differing versions of python
# will putting yaml in requirements.txt fix it
# indicative of environment management issues